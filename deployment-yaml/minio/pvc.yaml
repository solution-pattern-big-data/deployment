---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  namespace: stock-predict
  labels:
    app: minio
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
